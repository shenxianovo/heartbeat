# 06 — 未知数据退出恢复与原生验收

Status: needs-triage

**Blocked by:** 05；真实 Mac/Windows 与安装绑定环境。

## Problem

05 完成接纳、数据证据与最终动作的解耦。数据未知时会阻止安装器与原生退出，保留停止后的 owner
和结果；当前没有重试／强退 UI，因此可能一直无法自动退出。这是已确认的阶段性限制，不是完整
产品验收。离线但本地数据持久化必须继续允许退出。

## Decisions to resolve

- 退出期限和末次联网交付预算，以及达到期限后的呈现。
- 如何保留或移交本地保管责任以重试持久化；不能重新打开已 fenced 的 writer，也不能将重复 Stop
  当作恢复能力。当前退出结果复用且接纳关闭；新的恢复动作需有明确所有权。
- 未知数据的风险说明、显式强退和 OS 强制结束场景；不能因超时自动将未知视作安全。
- 与 01 原生 session 验收合并执行真实 Mac/Windows 退出和更新安装验证。

## Already confirmed

- 安全检查和清理之后才调度安装器，调度失败记录错误并正常退出；不恢复原采集会话。
- 正常退出和更新重启保持首个已接纳意图；最终副作用只执行一次。

## Acceptance

- [ ] 明确恢复交互、期限和剩余数据 owner，补齐失败／恢复行为验证。
- [ ] 真实 Mac/Windows：菜单退出、系统退出、重复退出、未知余量阻止退出。
- [ ] 安装绑定下的普通退出更新与更新重启；安装器失败正常退出，下次手动启动旧版本。
- [ ] 更新 PRD 与 ADR，区分自动验证、原生验收和完整产品行为完成。

## Verification

待裁决后实施；05 的自动测试不替代上述原生安装验收。
